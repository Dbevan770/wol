import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textInput: {
    color: 'black',
    padding: 10,
    borderWidth: 2,
    borderColor: 'black',
    borderRadius: 5,
    fontSize: 20,
  },
});
